<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">
    <!-- TODO: Change to Relative Layout, add ImageView (type), add ImageView (status) -->

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@android:color/darker_gray" />

    <TextView
        android:id="@+id/text_test_item"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="10dp"
        android:textSize="@dimen/textSuiteName"
        android:drawableLeft="@android:drawable/presence_busy"
        android:drawableRight="@drawable/ic_list"
        android:textAppearance="?android:attr/textAppearanceLarge" />

</LinearLayout>